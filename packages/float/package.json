{
  "name": "@emptyui/float",
  "version": "0.1.5",
  "description": "A flexible component for creating your own floating elements in React Native.",
  "author": "Tan Le",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/tanlethanh/emptyui.git",
    "directory": "packages/float"
  },
  "files": [
    "lib"
  ],
  "main": "lib/cjs/index.js",
  "module": "lib/esm/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "rimraf lib && npm run build:esm && npm run build:cjs",
    "build:esm": "tsc -p tsconfig.esm.json --outDir lib/esm",
    "build:cjs": "tsc -p tsconfig.cjs.json --outDir lib/cjs",
    "release": "yarn build && npm publish --access public"
  },
  "peerDependencies": {
    "@types/react": ">=18",
    "react": ">=18",
    "react-native": ">=0.72.0"
  },
  "dependencies": {
    "@emptyui/portal": "^0.1.1"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
